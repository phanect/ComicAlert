<div class="hero-unit">
	<h2 class="title"><%= comic.title %></h2>
	<%- linkTo('Edit this comic', editComicPath(params.id), {class: 'btn pull-right'}); %>
	<%- linkTo('Back to list', comicPath(''), {class: 'btn pull-right'}); %>
</div>

<h2>Comic Properties</h2>
<% for(var i in comic) { -%>
<% if(i !== 'saved') { -%>
<div class="row list-item">
	<div class="span4">
		<h3><%= i %></h3>
	</div>
	<div class="span8">
		<h3><i class="icon-list-alt"></i><span class="<%= i %>"><%= comic[i] %></span></h3>
	</div>
</div>
<% } -%><% } -%>